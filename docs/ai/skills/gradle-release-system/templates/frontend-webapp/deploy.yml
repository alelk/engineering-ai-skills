name: Deploy

on:
  workflow_dispatch:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Download release asset
        run: |
          echo "TODO: implement deployment for your target environment"
          echo "This workflow is intentionally a stub — customise it per project."
